{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && sed -i \"s|import \\* as zod from 'zod'|import * as zod from 'zod/v4'|g\" ../../lib/api-zod/src/generated/api.ts && npm --prefix ../.. run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
